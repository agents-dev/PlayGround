# Copy to .env (never commit .env). Keep the key outside the sandbox.
OPENAI_API_KEY=your-api-key-here
# Required scopes: api.agents.read, api.agents.write, api.responses.write
# SDKs send `OpenAI-Beta: agents=v1` automatically; curl must send it explicitly.
