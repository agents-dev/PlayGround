{
  "name": "playground-openai-hosted-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Demo of OpenAI-hosted sandboxes (Agents API, environment.type=openai_hosted)",
  "type": "module",
  "scripts": {
    "test": "node --test tests/test.mjs",
    "report": "node src/createReport.js"
  },
  "dependencies": {
    "openai": "^4.0.0"
  }
}
