# PlayGround — ⛏️ MiniCraft

A tiny Minecraft clone with zero build step. Open `index.html` or serve statically.

Run: `python3 -m http.server 8000` → http://localhost:8000

Controls: WASD move · mouse look · Space jump · F fly · left-click break · right-click place · 1–9/wheel select.